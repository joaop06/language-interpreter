type A = {
  COMMON: {
    NOT_FOUND: string;
    BAD_REQUEST: string;
    CONFLICT: string;
    ALREADY_EXISTS: string;
    INTERNAL_SERVER_ERROR: string;
    INVALID_TRANSLATE_CODE: string;
  };
};

export default A;
