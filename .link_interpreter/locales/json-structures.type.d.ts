/**
 * Types to represent the JSON structures of the files
 */
export type JsonTypes = En | Es;

export type JsonFilesType = "en" | "es";

/**
 * Structures of each JSON file represented in a specific type
 * @see En
 * @see Es
 */

export type En = {
  HELLO: string;
};
export type Es = {
  HELLO: string;
};
